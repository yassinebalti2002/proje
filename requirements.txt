fastapi==0.115.0
uvicorn==0.30.0
pydantic==2.7.0
python-multipart==0.0.9
scikit-learn==1.5.0
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2
pyod==2.0.0
mysql-connector-python==8.4.0
requests==2.32.3
scipy==1.13.1

# ── Sécurité production ────────────────────────────────────────────────────────
python-dotenv==1.0.1
cryptography==43.0.1   # generate_selfsigned_cert.py (TLS/HTTPS local ou reseau interne)
bcrypt==4.2.0           # hash des mots de passe utilisateurs (user_auth.py)
PyJWT==2.9.0            # tokens de session utilisateurs (user_auth.py)
email-validator==2.2.0  # requis par pydantic EmailStr (user_auth.py)

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest==8.3.5
httpx==0.27.0

# ── Modules optionnels pour fonctionnalités avancées ──────────────────────────

# Alertes SMS (optionnel — nécessite un compte Twilio)
# twilio>=8.0.0

# Export Edge / ONNX (optionnel — pour déploiement embarqué futur)
# skl2onnx>=1.16.0
# onnxruntime>=1.17.0
